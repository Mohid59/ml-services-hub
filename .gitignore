# Virtual envs
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# App runtime artifacts
uploads/
models/*.h5
models/*.keras
*.log
# ship the trained gender model so the live demo uses real weights (8 MB)
!models/gender_cnn.h5

# Training data (downloaded via services/prepare_gender_data.py)
data/

# Tooling caches
.pytest_cache/
.ruff_cache/

# HF / torch caches (if redirected into project)
.cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
